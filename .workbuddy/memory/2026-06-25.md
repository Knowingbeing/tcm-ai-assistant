# 2026-06-25

## 中医AI智能问诊助手 — P0 Supabase 升级完成

**项目路径**：`D:\用户文件勿删\Desktop\中医AI智能问诊助手\`
**GitHub**：https://github.com/Knowingbeing/tcm-ai-assistant
**技术栈**：Streamlit + Python + DeepSeek/OpenAI 兼容 API + **Supabase Postgres（新增）**

### 完成的工作
1. 设计并落地 Supabase 数据库 schema（4 张表：patients / consultations / settings / schema_version）
2. 封装 `utils/supabase_client.py`（单例管理 + 多源凭证 + 降级策略）
3. 改造 `app.py` 存储层（保持函数签名不变，上层 UI 零改动）
4. 编写 `scripts/migrate_json_to_supabase.py` 幂等迁移脚本
5. 编写 `scripts/smoke_test.py` 离线冒烟测试（6 段 18 断言全通过）
6. 更新 `requirements.txt` / `.env.example` / `.streamlit/secrets.toml.example` / `.gitignore`
7. 修复 `Dict` 未导入的真实 bug（`from typing import Dict, List`）
8. 重写 `HANDOFF.md`，含详细部署步骤

### 关键决策
- **降级策略**：Supabase 未配置时自动回退 JSON 模式，应用顶部有醒目标识
- **小步可逆**：保留 JSON 兜底，老用户不配 Supabase 也能用
- **数据隔离**：单租户设计，anon key 直连，RLS 已禁用
- **签名兼容**：load_records/save_records/load_settings/save_settings 四个函数保持原签名

### 验证
- 6 段冒烟测试全过（降级/JSON/导入/schema/迁移脚本/app.py 编译）
- app.py 顶层 import 成功
- Phase 1/2 配置切换测试通过

### 下一步
- P1：多轮问诊（追问式对话）
- P2：舌象识别 / 用户系统
- 用户在 Supabase 控制台执行 schema.sql + 配 secrets 后即可上线
